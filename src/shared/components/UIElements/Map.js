import React from "react";

import "./Map.css";

const Map = (props) => {
  return (
    <div className={`map ${props.className}`} style={props.style}>
      MAP WOULD BE HERE BUT I DONT LIKE GOOGLE
    </div>
  );
};

export default Map;
